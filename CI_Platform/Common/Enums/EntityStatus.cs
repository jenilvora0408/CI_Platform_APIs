using Common.Constants;


namespace Common.Enums
{
    public enum UserStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum CountryStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum CityStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum MissionThemeStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum SkillStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum MissionStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        CLOSED = EntityStatusConstant.CLOSED,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum MediaStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum BannerStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }
    public enum CMSPageStatus
    {
        ACTIVE = EntityStatusConstant.ACTIVE,
        INACTIVE = EntityStatusConstant.INACTIVE,
        DELETED = EntityStatusConstant.DELETED
    }
    public enum StoryStatus
    {
        DRAFT = EntityStatusConstant.DRAFT,
        PENDING = EntityStatusConstant.PENDING,
        APPROVED = EntityStatusConstant.APPROVED,
        DECLINED = EntityStatusConstant.DECLINED,
        DELETED = EntityStatusConstant.DELETED,
    }
    
    public enum CommentStatus
    {
        PENDING = EntityStatusConstant.PENDING,
        APPROVED = EntityStatusConstant.APPROVED,
        DECLINED = EntityStatusConstant.DECLINED,
        DELETED = EntityStatusConstant.DELETED
    }
    
    public enum TimesheetStatus
    {
        PENDING = EntityStatusConstant.PENDING,
        APPROVED = EntityStatusConstant.APPROVED,
        DECLINED = EntityStatusConstant.DECLINED,
        DELETED = EntityStatusConstant.DELETED
    }

    public enum MissionApplicationStatus
    {
        PENDING = EntityStatusConstant.PENDING,
        APPROVED = EntityStatusConstant.APPROVED,
        DECLINED = EntityStatusConstant.DECLINED,
        DELETED = EntityStatusConstant.DELETED
    }
}
